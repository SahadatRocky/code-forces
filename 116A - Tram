#include<bits/stdc++.h>
using namespace std;

int main(){

    int n,a,b,res=0,m=-1;
    cin>>n;
    while(n--){
        cin>>a>>b;
        res+=b-a;
          if(res>m){
              m=res;
          }
    }
  cout<<m<<endl;


   return 0;
}
